# 环境变量配置示例
# 复制此文件为 .env 并修改相应值

# 管理员账号配置（支持多个管理员）
# 方式1（推荐）：使用 ADMIN_ACCOUNTS 配置多个管理员
# 格式：用户名:密码，多个账号用逗号分隔
ADMIN_ACCOUNTS=admin1:your_password_1,admin2:your_password_2,admin3:your_password_3

# 方式2（向后兼容）：如果未设置 ADMIN_ACCOUNTS，则使用以下单个账号
# ADMIN_USER=admin
# ADMIN_PASS=your_secure_password_here

# 安全密钥（生产环境必须修改！）
SECRET_KEY=change_this_to_a_random_secret_key_in_production

# 服务器配置
SERVER_HOST=0.0.0.0
SERVER_PORT=5000
SERVER_URL=http://8.148.69.148:5000

# 调试模式（生产环境设为False）
DEBUG=False

# SQLite缓存大小（单位：KB，负值表示MB）
# 针对短期高并发场景，建议256MB（-256000）
# 如果服务器内存充足，可以设置为512MB（-512000）
SQLITE_CACHE_SIZE=-256000

# Gunicorn配置（如果使用gunicorn）
GUNICORN_WORKERS=4
LOG_LEVEL=info
